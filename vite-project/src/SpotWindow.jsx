import React from "react";
import { useNavigate } from "react-router-dom";

export default function spotWindow() {
        const navigate = useNavigate()

    return (
        <div className = 'spotContainer'>
            <p>hello world!</p>
        </div>
    )
}
